Get messages
---
tags: [message]
description: Get list of messages
parameters:
  - in: query
    name: token
    description: jwt
    required: true
    type: string

responses:
  200:
    description: List of messages
    schema:
      type: array
      items:
        $ref: '#/definitions/MessageDTO'

