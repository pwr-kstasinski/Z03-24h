Get online users login
---
tags: [user]
description: Get list of online users login
parameters:
  - in: query
    name: token
    description: jwt
    required: true
    type: string

responses:
  200:
    description: List of users
    schema:
      type: array
      items:
        type: string

